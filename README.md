To compile the paper, first install texlive and texline-fonts-extra:

```
$ sudo apt-get install texlive
$ sudo apt-get install texlive-fonts-extra
```

Clone the paper:

```
git clone git@github.com:Waziup/GreenIoT-paper.git
```

Then compile it:

```
$ cd GreenIoT-paper
$ make
```
